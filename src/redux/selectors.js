export const getFilmsSelector = (state) => state.repos.allFilms;

export const getSelectedFilmSelector = (state) => state.repos.selectedFilm;

export const loadingSelector = (state) => state.repos.loading;

export const getAllCharactersSelector = (state) => state.repos.allCharacters;

export const getCharacterSelector = (state) => state.repos.character;
